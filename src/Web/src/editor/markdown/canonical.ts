/** List indent used by the official serializer. Chosen from official default, GFM, and SampleVault YAML/list spacing — not because it is merely the package default. */
export const CANONICAL_LIST_INDENT = 2

export type MarkdownEngineId = 'official'
